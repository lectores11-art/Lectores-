/** One shared invite URL; this is the use cap, not the number of links. */
export const DEFAULT_INVITE_MAX_USES = 200;
export const DEFAULT_INVITE_TTL_DAYS = 30;
export const INVITE_JOIN_PER_IP = 60;
export const INVITE_JOIN_WINDOW_MS = 60_000;
